import { StyleSheet } from "react-native";

export const CL = StyleSheet.create({
  flex1: { flex: 1 },
  row: { flexDirection: 'row' },
  center: { justifyContent: 'center', alignItems: 'center' },
});

export const CC = StyleSheet.create({
  bgWhite: { backgroundColor: '#FFFFFF' },
});