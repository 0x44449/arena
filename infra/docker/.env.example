# ============================================
# 공통 (DB)
# ============================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=arena
POSTGRES_USER=arena
POSTGRES_PASSWORD=arena

# ============================================
# 외부 포트 (로컬에서 Docker 접근용)
# ============================================
EXTERNAL_POSTGRES_PORT=15432
EXTERNAL_REDIS_PORT=16379
EXTERNAL_API_PORT=18080
EXTERNAL_MINIO_PORT=19000
EXTERNAL_MINIO_CONSOLE_PORT=19001

# ============================================
# API (Spring Boot)
# ============================================
API_LOG_LEVEL=INFO
SUPABASE_JWKS_URI=

# ============================================
# MinIO (S3 호환 Object Storage)
# ============================================
MINIO_ROOT_USER=arena
MINIO_ROOT_PASSWORD=arena123
MINIO_BUCKET_PUBLIC=arena-files-public
MINIO_BUCKET_PRIVATE=arena-files-private
